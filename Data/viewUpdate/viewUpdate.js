const viewUpdate ={
    message:"SELECT FROM BELOW TO VIEW EMPLOYEE ?",
    type:"rawlist",
    name:"viewUpdateChoices",
    choices:[

        "---------UPDATE BY ROLE-----------",
        "---------UPDATE BY MANAGER-------------",
        "---------EXIT----------"
    ]
}

module.exports=viewUpdate;